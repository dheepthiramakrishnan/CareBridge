# Not used

This is an earlier, incomplete Spring Boot prototype (3 controllers, all
returning hardcoded fake data, no real database wiring at all). It is kept
here only for reference. The actual backend for this project is the
Node/Express + MySQL server one level up (`../server.js`).
